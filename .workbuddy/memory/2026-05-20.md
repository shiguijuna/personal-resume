# 2026-05-20

## personal-resume 项目全量优化

按分析报告的 9 项优化全部执行，每项独立提交推送至 GitHub。

### 完成的优化列表
1. **提取共享 SCSS 样式** — 创建 `shared.scss`，消除 7 组件间 ~270 行重复代码
2. **修复硬编码颜色** — `projects/[slug]`、`SkillsSection`、`ContactSection` 中硬编码颜色全部改为 CSS Variables
3. **拆分 AppHeader** — 740 行单文件拆为 `AppHeader`(壳) + `ThemePicker` + `MobileDrawer`
4. **重构 TechIcon** — 13 路 v-if 链改为 `Map<string, FunctionalComponent>` O(1) 查找
5. **404 错误页** — 新增 `error.vue` 自定义错误页
6. **@nuxt/image** — 接入图片优化模块，自动 WebP 转换
7. **@nuxtjs/sitemap** — 替换手写 sitemap.xml 为自动生成
8. **RSS 订阅源** — `server/api/rss.xml.ts` 提供 RSS 2.0 订阅
9. **GitHub Actions CI** — `.github/workflows/ci.yml` 自动构建流水线

### 技术要点
- 所有 `<img>` 替换为 `<NuxtImg>` 以启用 WebP 转换
- 共享 SCSS 通过 `@use` + `@include` 引入，保持 scoped 隔离
- ContactSection 中修复了 BlogSection 残留的 `.blog-feature-layout` 媒体查询 Bug
